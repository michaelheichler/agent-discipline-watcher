---
phase: 1
plan: 1
title: protected.py path classification hardening (plugin exemption, symlink resolve, client-home root delete)
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
skills_used: [agent-skills:security-and-hardening, agent-skills:test-driven-development]
files_modified: [hooks/lib/protected.py, hooks/lib/test_protected.py, hooks/test_pre_bash.py, hooks/test_self_protection_invariants.py]
files_touched: [hooks/lib/protected.py, hooks/lib/test_protected.py, hooks/test_pre_bash.py, hooks/test_self_protection_invariants.py]
forbidden_commands: []
must_haves:
  truths:
    - "path_findings() on a write under ~/.claude/plugins/** returns a live_client_surface finding (REQ-01)"
    - "A write issued through a symlink whose real target sits inside a CLAUDE_WIRING_DIRS path is classified live_client_surface"
    - "A mutating command (rm/unlink/shred) naming a bare client home root (~/.claude, ~/.codex, ~/.pi) produces a finding from pre_bash.command_findings"
    - "test_home_root_itself_is_not_a_surface still passes: the sandbox home root itself stays unflagged for the Write path"
    - "Full suite stays at baseline: 978 passed, only the pre-existing test_plugin_wiring strict-validator failure remains (do not fix it)"
  artifacts:
    - {path: "hooks/lib/protected.py", provides: "hardened path classifier", contains: "realpath"}
    - {path: "hooks/lib/test_protected.py", provides: "regression tests for all three fixes", contains: "plugins"}
    - {path: "hooks/test_pre_bash.py", provides: "rm -rf client-home-root classification test", contains: "rm -rf"}
  key_links:
    - {from: "hooks/pre_bash.py", to: "hooks/lib/protected.py", via: "is_live_client_path -> _live_client_rule (shared classifier, both callers must pick up every fix)"}
    - {from: "hooks/pre_write.py", to: "hooks/lib/protected.py", via: "path_findings -> _resolve/_normalize"}
---
<objective>
Close the two Critical self-tamper bypasses in hooks/lib/protected.py (plugin-root exemption, symlink dereference gap) and classify destructive deletes of a bare client home root. All three fixes land in the shared classifier so pre_write.py (Write/Edit), pre_bash.py redirect checks, and pre_bash.py mutating-verb checks pick them up from one place. REQ-01.
</objective>
<context>
@.vbw-planning/phases/01-self-tamper-fail-open-hardening/01-RESEARCH.md
@/Users/michael/.claude/plugins/cache/addy-agent-skills/agent-skills/7829ffd90d97/skills/security-and-hardening/SKILL.md
@/Users/michael/.claude/plugins/cache/addy-agent-skills/agent-skills/7829ffd90d97/skills/test-driven-development/SKILL.md

Research items 1, 2, and 5 (all confidence: confirmed, item 5 by static trace only because Scout Bash was read-only). Key constraints from the research Risks section:
- The symlink fix must keep working for a target file that does not exist yet (path_findings supports not-yet-created paths, docstring on _resolve line 107). It must also keep matching a sandbox HOME that itself lives under a symlinked temp dir (macOS /var -> /private/var). Resolve existing symlink components, leave nonexistent trailing components lexical.
- The plugin-exemption fix removes "plugins" from CLAUDE_EXEMPT_DIRS entirely. The phase success criterion says "plugin-root exemption removed". The shipped test test_non_wiring_paths_pass[".claude/plugins/cache/other/hooks/hooks.json"] locks in the old behavior. It must be updated to expect a finding.
- The client-home-root fix must NOT regress test_home_root_itself_is_not_a_surface. "Path is the sandbox home root" stays fine. "Path is .claude/.codex/.pi directly under home" becomes classifiable. These currently collapse into the same len(parts) < 2 short-circuit in _reaches_into_a_client_home/_claude_rule.
Test command: PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q
</context>
<tasks>
<task type="auto">
  <name>Remove the plugins exemption from CLAUDE_EXEMPT_DIRS</name>
  <files>
    hooks/lib/protected.py
    hooks/lib/test_protected.py
  </files>
  <action>
Remove "plugins" from CLAUDE_EXEMPT_DIRS (protected.py lines 21-26) and adjust _claude_rule so paths under ~/.claude/plugins/** with at least one segment past "plugins" classify as live_client_surface. Update the stale rationale comment above CLAUDE_EXEMPT_DIRS. Update test_non_wiring_paths_pass to drop the ".claude/plugins/cache/other/hooks/hooks.json" parameter. Add a test asserting a write to ~/.claude/plugins/cache/agent-discipline-watcher/hooks/pre_write.py yields a live_client_surface finding. Add a second test asserting a write to any other plugin's cache path also yields one. The exemption is removed for all plugins, per the success criterion. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/lib/test_protected.py -q
  </verify>
  <done>
path_findings flags the live plugin-cache path, updated test file passes, one atomic commit.
  </done>
</task>
<task type="auto" correctness="dijkstra">
  <name>Dereference existing symlinks in _resolve before classification</name>
  <files>
    hooks/lib/protected.py
    hooks/lib/test_protected.py
  </files>
  <action>
Extend _resolve/_normalize so that after lexical dot-segment collapse, any existing symlink component is dereferenced (os.path.realpath on the deepest existing ancestor, then re-append the nonexistent tail lexically) before prefix comparison. Invariant to state and prove: for every input path P, classify(P) equals classify of the realpath of P's deepest existing ancestor joined with P's lexical tail. Preserve both documented behaviors: not-yet-existing targets still classify, and a sandbox HOME under a symlinked temp dir still matches (compare against the resolved home as well). Both path_findings (Write/Edit) and is_live_client_path (Bash) funnel through this code, so one fix covers both callers. Add three tests. First, a real os.symlink outside home pointing at a CLAUDE_WIRING_DIRS file is classified live_client_surface. Second, a nonexistent target path still classifies. Third, a home under a symlinked tmpdir still matches. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/lib/test_protected.py hooks/test_pre_bash.py -q
  </verify>
  <done>
Symlink bypass test passes, no regression in existing _resolve/_normalize tests, one atomic commit.
  </done>
</task>
<task type="auto" correctness="dijkstra">
  <name>Classify mutating commands on a bare client-home root</name>
  <files>
    hooks/lib/protected.py
    hooks/test_pre_bash.py
    hooks/lib/test_protected.py
    hooks/test_self_protection_invariants.py
  </files>
  <action>
In the shared _live_client_rule/_reaches_into_a_client_home machinery, distinguish "path IS the sandbox home root" (stays unclassified, test_home_root_itself_is_not_a_surface must keep passing) from "path is a client home dir (.claude, .codex, .pi) sitting directly under home with no further segments" (must classify, so a mutating verb against it fires). Do NOT patch pre_bash._deletes_state, it is scoped to project-local .agent-discipline state per research recommendation 5. Add tests in hooks/test_pre_bash.py asserting rm -rf ~/.claude, rm -rf ~/.codex, unlink and shred variants produce a finding from command_findings, and in test_protected.py for the classifier boundary (home root gives no finding, home/.claude gives a finding). This item was only statically traced by Scout, so the new test is the first live execution of the fix path: run it and paste the pass output, no claim without a run. Update hooks/test_self_protection_invariants.py if the classifier change shifts its expectations. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/test_pre_bash.py hooks/lib/test_protected.py hooks/test_self_protection_invariants.py -q && PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q
  </verify>
  <done>
rm -rf on a protected root is classified, test_home_root_itself_is_not_a_surface still passes, full suite at baseline (978 passed, 1 pre-existing unrelated failure), one atomic commit.
  </done>
</task>
</tasks>
<verification>
1. PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q shows 978+new passed and only the pre-existing test_plugin_wiring strict-validator failure.
2. Replaying the research repro from hooks/lib/: path_findings on home/.claude/plugins/cache/agent-discipline-watcher/hooks/pre_write.py returns a live_client_surface finding (was []).
3. Replaying the symlink repro: findings via a symlink outside home now match findings on the direct protected path.
</verification>
<success_criteria>
- path_findings() flags a Write to the live plugin-cache path (plugin-root exemption removed)
- protected.py resolves symlinks and collapses .. before comparing against protected targets
- rm -rf on a protected root directory (~/.claude, ~/.codex) is classified
- One commit per task, three commits total
</success_criteria>
<output>
01-01-SUMMARY.md
</output>
