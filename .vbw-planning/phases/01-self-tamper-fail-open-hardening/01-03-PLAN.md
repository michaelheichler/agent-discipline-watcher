---
phase: 1
plan: 3
title: First-config-creation validation (reject rule-family kill and state/ledger root redirection)
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
cross_phase_deps: []
autonomous: true
skills_used: [agent-skills:security-and-hardening, agent-skills:test-driven-development]
files_modified: [hooks/lib/protected.py, hooks/lib/config.py, hooks/lib/test_protected.py, hooks/lib/test_config_schema.py, hooks/test_self_protection_invariants.py]
files_touched: [hooks/lib/protected.py, hooks/lib/config.py, hooks/lib/test_protected.py, hooks/lib/test_config_schema.py, hooks/test_self_protection_invariants.py]
forbidden_commands: []
must_haves:
  truths:
    - "grants_escape returns True for a payload that disables every rule family, via the boolean switches or the gates map (REQ-01)"
    - "grants_escape returns True for a payload that sets state_root or ledger_root"
    - "path_findings on a not-yet-existing .agent-discipline.json with such a payload returns a finding, first creation is no longer unconditionally allowed"
    - "A benign config that disables one rule family still creates cleanly, no new false blocks"
    - "Full suite stays at baseline plus new tests, only the pre-existing test_plugin_wiring strict-validator failure remains"
  artifacts:
    - {path: "hooks/lib/protected.py", provides: "escape detection covering family switches, gates map, and root redirection", contains: "grants_escape"}
    - {path: "hooks/lib/test_protected.py", provides: "regression tests replaying the research attack payload", contains: "state_root"}
  key_links:
    - {from: "hooks/lib/protected.py", to: "hooks/lib/config.py", via: "grants_escape -> flatten_settings, and the family/gate key names must match config.DEFAULTS"}
    - {from: "hooks/pre_write.py", to: "hooks/lib/protected.py", via: "path_findings content gate applied to config writes, including first creation"}
---
<objective>
Close the first-run config bypass. Today a single Write can create .agent-discipline.json with every rule family disabled and state_root/ledger_root pointed at an attacker-writable path. It clears with zero findings. _is_config_seal only fires for a file that already exists, and grants_escape never inspects those keys. Extend the content gate so that payload is rejected at creation time. REQ-01.
</objective>
<context>
@.vbw-planning/phases/01-self-tamper-fail-open-hardening/01-RESEARCH.md
@/Users/michael/.claude/plugins/cache/addy-agent-skills/agent-skills/7829ffd90d97/skills/security-and-hardening/SKILL.md
@/Users/michael/.claude/plugins/cache/addy-agent-skills/agent-skills/7829ffd90d97/skills/test-driven-development/SKILL.md

Research item 4, confirmed with a live repro (see Live Validation Evidence item 5). Design constraints:
- The phase success criterion is scoped: reject a payload that disables EVERY rule family, or that redirects state/ledger roots. Disabling a single family is a legitimate user action and must keep working.
- Both the boolean switches (punctuation/english/clean_code) and the newer gates map can disable a family. The check must consider them together when deciding "every family is off".
- state_root and ledger_root are not rule names, so they do not fit the existing rule_gates-shaped check in grants_escape. Cover the two keys explicitly (research risk note 3).
- Sequence per research recommendation 4: extend grants_escape first (task 1), then make the content gate fire on first creation (task 2). Deeper DEFAULTS/effective_config schema validation is out of scope for this phase.
- This plan modifies files plan 01-01 also touched (protected.py, test_protected.py, test_self_protection_invariants.py), which is why it runs in a later wave.
Test command: PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q
</context>
<tasks>
<task type="auto">
  <name>Extend grants_escape to cover family kill and root redirection</name>
  <files>
    hooks/lib/protected.py
    hooks/lib/test_protected.py
  </files>
  <action>
Extend grants_escape (protected.py lines 48-59) to return True when the flattened settings disable every rule family (all of punctuation, english, and clean_code turned off through the boolean switches, the gates map, or a mix of the two) or when state_root or ledger_root is present. Keep the existing AUTH_KEY and rule_gates checks unchanged. Add tests replaying the exact research attack payload (grants_escape must now return True for it), plus negatives: one family disabled returns False, and a payload without roots or family kills returns False. Key names must match config.DEFAULTS, read hooks/lib/config.py lines 37-67 before coding. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/lib/test_protected.py -q
  </verify>
  <done>
Attack payload flips grants_escape to True, benign payloads stay False, tests pass, one atomic commit.
  </done>
</task>
<task type="auto">
  <name>Apply the content gate on first config creation</name>
  <files>
    hooks/lib/protected.py
    hooks/lib/test_protected.py
  </files>
  <action>
Make path_findings apply the escape-content check to a gate config that does not exist yet, so first creation is no longer unconditionally allowed. Today _is_config_seal (lines 183-190) requires path.exists(). Route the grants_escape content check for gate-config paths independent of existence, while keeping the seal rule (blocking edits to an existing config) semantically separate from the new first-creation content rule. Add a test replaying the research repro: path_findings(cfg_path, None, home, attack_json) on a nonexistent cfg_path must return a finding instead of []. Also test that creating a benign config still returns no findings. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/lib/test_protected.py -q
  </verify>
  <done>
First creation of the attack payload yields a finding, benign first creation stays clean, one atomic commit.
  </done>
</task>
<task type="auto">
  <name>Invariant and schema regression pass</name>
  <files>
    hooks/test_self_protection_invariants.py
    hooks/lib/test_config_schema.py
  </files>
  <action>
Run the self-protection invariant suite and the config schema suite against the new behavior. Extend hooks/test_self_protection_invariants.py with an invariant asserting the research attack payload can never be created with zero findings. Update hooks/lib/test_config_schema.py only if the grants_escape key coverage changed an expectation there. Then run the full suite and confirm baseline plus new tests, with only the pre-existing test_plugin_wiring strict-validator failure. Do not fix that failure. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/test_self_protection_invariants.py hooks/lib/test_config_schema.py -q && PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q
  </verify>
  <done>
Invariant test locks the fix in, full suite at baseline plus new tests, one atomic commit.
  </done>
</task>
</tasks>
<verification>
1. Replaying research Live Validation Evidence item 5: grants_escape(attack_json) is True and path_findings on the nonexistent config path returns a finding.
2. A benign config write (single family off, no roots) still creates with zero findings.
3. PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q shows baseline plus new tests passing.
</verification>
<success_criteria>
- First-config-creation rejects a payload that disables every rule family or redirects state/ledger roots
- Single-family disables and benign configs keep working
- One commit per task, three commits total
</success_criteria>
<output>
01-03-SUMMARY.md
</output>
