---
phase: 1
plan: 2
title: Fail-closed malformed-payload handling across all four PreToolUse hooks
type: execute
wave: 2
depends_on: ["01-01"]
cross_phase_deps: []
autonomous: true
skills_used: [agent-skills:security-and-hardening, agent-skills:test-driven-development]
files_modified: [hooks/lib/hookio.py, hooks/pre_write.py, hooks/pre_bash.py, hooks/pre_commit.py, hooks/pre_mcp.py, hooks/lib/test_hookio.py, hooks/test_hooks.py, hooks/test_pre_bash.py]
files_touched: [hooks/lib/hookio.py, hooks/pre_write.py, hooks/pre_bash.py, hooks/pre_commit.py, hooks/pre_mcp.py, hooks/lib/test_hookio.py, hooks/test_hooks.py, hooks/test_pre_bash.py]
forbidden_commands: []
must_haves:
  truths:
    - "hookio distinguishes a JSON parse failure from empty stdin: the two cases produce different values (REQ-01)"
    - "pre_write, pre_bash, pre_commit, and pre_mcp each return a deny decision when stdin held malformed JSON"
    - "Empty stdin (genuinely nothing to check) still returns allow on all four hooks, no new false blocks"
    - "pre_mcp still allows a well-formed payload with an empty session_id (legitimate session-less events keep working)"
    - "Full suite stays at baseline plus new tests, only the pre-existing test_plugin_wiring strict-validator failure remains"
  artifacts:
    - {path: "hooks/lib/hookio.py", provides: "parse-failure-aware payload reader", contains: "read_payload"}
    - {path: "hooks/lib/test_hookio.py", provides: "first regression coverage for hookio (none exists today)", contains: "malformed"}
    - {path: "hooks/test_hooks.py", provides: "end-to-end deny-on-malformed tests for the entry scripts", contains: "deny"}
  key_links:
    - {from: "hooks/lib/hookio.py", to: "hooks/pre_write.py", via: "read_payload return contract consumed by every entry script main()"}
    - {from: "hooks/lib/hookio.py", to: "hooks/pre_mcp.py", via: "read_payload return contract, with the session-less allow path preserved"}
---
<objective>
Make a malformed hook payload fail closed. Today hookio.read_payload() swallows the JSON error and returns {}, which every entry script treats as "nothing to check" and allows. Give the parse failure a distinct signal and have all four PreToolUse entry scripts deny on it, without breaking the legitimate empty-stdin and session-less-MCP allow paths. REQ-01.
</objective>
<context>
@.vbw-planning/phases/01-self-tamper-fail-open-hardening/01-RESEARCH.md
@/Users/michael/.claude/plugins/cache/addy-agent-skills/agent-skills/7829ffd90d97/skills/security-and-hardening/SKILL.md
@/Users/michael/.claude/plugins/cache/addy-agent-skills/agent-skills/7829ffd90d97/skills/test-driven-development/SKILL.md

Research item 3, live-verified end to end for all four scripts. Design constraints:
- allow() is also {}, so the parse-failure signal cannot be {}. The mechanism is Dev's choice (sentinel object, raised exception the existing try/except deny wrapper catches, or a second return value). It must reach all four call sites.
- Each entry script already wraps run() in try/except Exception returning deny(UNDECIDABLE + str(exc)). Raising from read_payload only fails closed if the read happens inside that wrapper. Check where read_payload is called relative to the wrapper before choosing the mechanism.
- pre_mcp treats an empty session_id as a legitimate allow path for real session-less events. The fix must deny only on parse failure, not on every session-less event (research recommendation 3).
- There is no test_hookio.py today. Create hooks/lib/test_hookio.py.
Test command: PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q
</context>
<tasks>
<task type="auto" correctness="dijkstra">
  <name>Give read_payload a distinct parse-failure signal</name>
  <files>
    hooks/lib/hookio.py
    hooks/lib/test_hookio.py
  </files>
  <action>
Change hookio.read_payload() so a JSON parse failure is distinguishable from empty stdin. Postcondition to state first: empty stdin maps to the empty-payload value, valid JSON maps to the parsed dict, malformed JSON maps to the distinct failure signal, and no input maps to a silent {}. Keep the stderr diagnostic line. Create hooks/lib/test_hookio.py covering all three input classes. This is the module's first test file. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/lib/test_hookio.py -q
  </verify>
  <done>
Three input classes produce three distinguishable outcomes, new test file passes, one atomic commit.
  </done>
</task>
<task type="auto">
  <name>Deny on parse failure in pre_write, pre_bash, and pre_commit</name>
  <files>
    hooks/pre_write.py
    hooks/pre_bash.py
    hooks/pre_commit.py
    hooks/test_hooks.py
    hooks/test_pre_bash.py
  </files>
  <action>
Wire the new parse-failure signal into pre_write.py, pre_bash.py, and pre_commit.py so each returns a deny decision (reusing the existing UNDECIDABLE deny shape) when stdin held malformed JSON. Empty stdin must still allow. Add tests in hooks/test_hooks.py (and hooks/test_pre_bash.py where pre_bash coverage lives) that feed the exact malformed-stdin repro from the research ("not json {") through each script's entry path and assert a deny. Also assert the empty-stdin allow path is unchanged. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/test_hooks.py hooks/test_pre_bash.py -q
  </verify>
  <done>
All three scripts deny on malformed stdin and allow on empty stdin, tests pass, one atomic commit.
  </done>
</task>
<task type="auto">
  <name>Deny on parse failure in pre_mcp without blocking session-less events</name>
  <files>
    hooks/pre_mcp.py
    hooks/test_hooks.py
  </files>
  <action>
Wire the parse-failure signal into pre_mcp.py. It must deny on malformed JSON but keep allowing a well-formed payload whose session_id is empty or absent, since that is the intentional session-less path. Add tests for both cases plus the empty-stdin case. Then run the full suite and confirm baseline: 978 passed plus this phase's new tests, with only the pre-existing test_plugin_wiring strict-validator failure. Do not fix that failure. REQ-01.
  </action>
  <verify>
PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks/test_hooks.py -q && PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q
  </verify>
  <done>
pre_mcp denies malformed payloads, session-less events still allow, full suite at baseline plus new tests, one atomic commit.
  </done>
</task>
</tasks>
<verification>
1. Replaying the research repro (stdin "not json {") through pre_write.run, pre_bash.run, pre_commit.run, and pre_mcp.run entry paths yields a deny decision from each, not {}.
2. Empty stdin still yields allow from all four.
3. PYTHONPATH="hooks:hooks/lib" python3 -m pytest hooks -q shows baseline plus new tests passing.
</verification>
<success_criteria>
- hookio.py distinguishes a JSON parse failure from an empty object
- pre_write.py, pre_bash.py, pre_commit.py, pre_mcp.py all deny on that distinct failure
- No new false blocks on empty stdin or session-less MCP events
- One commit per task, three commits total
</success_criteria>
<output>
01-02-SUMMARY.md
</output>
