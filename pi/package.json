{
  "name": "agent-discipline-watcher-pi",
  "private": true,
  "description": "Declares the OMP packages the extension imports at runtime, so a clean checkout can run its tests.",
  "devDependencies": {
    "@oh-my-pi/pi-tui": "^18.0.11"
  }
}
